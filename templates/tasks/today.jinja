<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Today</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{url_for('static', filename='/css/global.css')}}">
</head>

<body>
    {% include 'components/side_bar.jinja' %}
    <div class="page-content">
        <h3>Tasks</h3>
        <div class="d-flex flex-wrap p-2">
            {% for t in tasks %}
            {% set bg = 'danger' if t.status == 'pending' else 'success' %}
            <div class="d-flex align-items-center flex-column bg-{{bg}} bg-gradient p-2 w-25 rounded shadow m-2">
                <h3 class="text-light">{{t.title}}</h3>
                <div class="w-100 text-light">
                    <strong>Client: </strong>
                    <span>{{ t.user.name }}</span><br>
                    <strong>Email: </strong>
                    <span>{{ t.client.email }}</span><br>
                    <strong>Status:</strong>
                    <span>{{ t.status }}</span><br>
                    <strong>Due date:</strong>
                    <span>{{ t.due_date }}</span>
                </div>
                <a class="btn btn-light mt-1 w-100 shadow" href="/task/details/{{t.id}}">
                    Open
                </a>
            </div>
            {% endfor %}
        </div>
    </div>
</body>
<script>

</script>
</html>