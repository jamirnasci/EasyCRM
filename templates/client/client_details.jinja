<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Client Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{url_for('static', filename='/css/global.css')}}">
</head>

<body>
    {% include 'components/side_bar.jinja' %}
    <div class="page-content">
        <div class="p-3">
            <h3>Update Client</h3>
            <form method="POST" action="/client/update/{{ client.id }}" class="w-75">
                <div class="mb-3">
                    <label for="name" class="form-label">Full Name</label>
                    <input type="text" class="form-control form-control-sm" id="name" name="name"
                        placeholder="Nome do cliente" value="{{client.name}}" required>
                </div>

                <div class="mb-3 d-flex">
                    <div>
                        <label for="email" class="form-label">E-mail</label>
                        <input type="email" class="form-control form-control-sm" id="email" name="email"
                            placeholder="email@cliente.com" value="{{client.email}}" required>
                    </div>
                    <div class="ms-2">
                        <label for="phone" class="form-label">Phone</label>
                        <input type="tel" class="form-control form-control-sm" id="phone" name="phone"
                            placeholder="(00) 00000-0000" value="{{client.phone}}">
                    </div>
                </div>
                <div class="mb-3">
                    <label for="status">Status</label>
                    <select class="form-select form-select-sm" name="status" id="status">
                        <option value="lead">Lead</option>
                        <option value="opportunity">Opportunity</option>
                        <option value="client">Client</option>
                        <option value="lost">Lost</option>
                    </select>
                </div>

                <div class="mb-3 d-flex">
                    <div>

                        <label for="company" class="form-label">Empresa</label>
                        <input type="text" class="form-control form-control-sm" id="company" name="company"
                            placeholder="Nome da empresa" value="{{client.company}}">
                    </div>
                    <div class="ms-2">
                        <label class="form-label" for="position">Position</label>
                        <input class="form-control form-control-sm" type="text" name="position" id="position" value="{{client.position}}">
                    </div>
                </div>
                <button type="submit" class="btn btn-success">Update</button>
            </form>
            <a class="btn btn-danger mt-1" href="/client/delete/{{client.id}}">Delete Client</a>
        </div>
    </div>
</body>

</html>